<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/secondaryProgress">
        <shape
            android:innerRadius="155dp"
            android:shape="ring"
            android:thicknessRatio="80.0"
            android:useLevel="true">

            <gradient
                android:centerColor="@color/hola_purple"
                android:endColor="@color/hola_purple"
                android:startColor="@color/hola_purple"
                android:type="sweep" />
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%"
            android:toDegrees="270">

            <shape
                android:innerRadius="160dp"
                android:shape="ring"
                android:thicknessRatio="400.0"
                android:useLevel="true">

                <rotate
                    android:fromDegrees="0"
                    android:pivotX="50%"
                    android:pivotY="50%"
                    android:toDegrees="360" />

                <gradient
                    android:centerColor="@color/yellow"
                    android:endColor="@color/yellow"
                    android:startColor="@color/yellow"
                    android:type="sweep" />

            </shape>
        </rotate>
    </item>
</layer-list>